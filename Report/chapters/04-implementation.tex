\chapter{Implementation Results}

This chapter documents the practical realization of the design presented in Chapter~\ref{chapter:METHODOLOGY AND DESIGN} and the observable outcomes of that implementation. Screenshots, file structures, and concrete configuration details are presented here to demonstrate that each designed component was built in a working form, and that the platform operates end-to-end as intended.

\section{Development Environment Setup}

\subsection{Monorepo Structure}
The repository follows a Turborepo layout with pnpm workspaces, organizing the codebase into three top-level directories: \texttt{apps/}, \texttt{packages/}, and \texttt{tooling/}. The structure is shown below:
\thesisfigure{ch4-monorepo-structure.png}{Repository Folder Structure}{Repository Folder Structure}{}{monorepo-structure}
This structure departs from the stock T3 Turbo reference layout in two deliberate ways. First, there are no Two structural decisions departing from the stock T3 Turbo layout. First, UI components are co-located within each app rather than shared through a ui package, because the mobile app (React Native) and the admin portal (Medusa's component system) do not share a rendering environment. Second, the admin application is excluded from the pnpm workspace and installed independently using npm (recorded as Architecture Decision Record 0002), because MedusaJS's dependency resolution expectations conflict with pnpm's strict workspace semantics.

\subsection{CI/CD Pipeline}
The CI/CD pipeline is hosted on Bitbucket Pipelines and is structured as two tracks: a pull-request pipeline and a set of branch-and-tag-triggered deployment pipelines.

On every pull request, the pipeline runs \texttt{pnpm install} followed by \texttt{pnpm audit -{}-audit-level=high} to scan for high-severity dependency vulnerabilities. Code quality enforcement (linting, type-checking, and formatting) is handled locally through Husky pre-commit hooks rather than as a CI gate.

Three deployment environments are defined — QA, UAT, and Production — each configured as a Bitbucket Deployment with its own environment variables:

\begin{itemize}
    \item \textbf{\texttt{main} branch} — auto-deploys the Next.js app to QA via Vercel, auto-applies Supabase migrations to the QA project, and exposes a manual trigger for an Expo EAS build using the \texttt{qa} profile.
    \item \textbf{\texttt{uat/v*} tags} — force-push the tagged commit to the \texttt{uat} environment branch, apply Supabase migrations to UAT, and expose a manual EAS build trigger using the \texttt{uat} profile.
    \item \textbf{\texttt{prod/v*} tags} — require a manual confirmation step, then force-push to the \texttt{prod} branch, apply Supabase migrations to Production, and expose a manual EAS build trigger using the \texttt{production} profile.
\end{itemize}

\textcolor{red}{\textbf{TODO: Change Inline Ref}} \\

Although Chapter 2 (Section 2.1.9.2) introduces Git Flow as a reference branching methodology, the branching model used in practice is trunk-based development with tag-triggered environment promotions. All feature work merges into main through pull requests; main auto-deploys to QA; promotions to UAT and Production are performed by creating annotated Git tags (uat/v* and prod/v*), which the CI pipeline reacts to by force-pushing the tagged commit to the corresponding environment branch. The uat and prod branches therefore function as environment markers rather than merge targets, and are protected through Bitbucket branch restrictions that permit write access only to a dedicated deployment bot user.
\thesisfigure{ch4-ci-cd-pipeline.png}{Bitbucket Pipelines Dashboard}{Bitbucket     
  Pipelines Dashboard Showing a Mix of PR Audit Runs and Tag-Triggered Deployments to
   QA/UAT/Production}{}{ci-cd-pipeline}


\subsection{Environment Isolation}

Three entirely separate Supabase projects — \texttt{[project]-qa}, \texttt{[project]-uat}, and \texttt{[project]-prod} — are provisioned, each with its own URL, anonymous key, service-role secret, and Postgres connection string. This guarantees that no environment can accidentally access data belonging to another.

Secrets and configuration values are managed across three layers:

\begin{itemize}
    \item \textbf{Vercel environment variables} per environment slot, for the Next.js applications.
    \item \textbf{Bitbucket repository and deployment variables}, for CI-side credentials.
    \item \textbf{EAS environment variables} per build profile, defined in \texttt{apps/expo/eas.json}.
\end{itemize}

Local development uses gitignored \texttt{.env} files copied from checked-in \texttt{.env.example} templates. No dedicated secret management service is used; the combination of these three layers covers the full surface of runtime secrets without an additional dependency.

Docker is used only as a local development dependency for running the Supabase stack (Postgres, Kong, Auth, Storage, Studio, Inbucket) via the Supabase CLI. The application services themselves are not containerized — no \texttt{Dockerfile} or \texttt{docker-compose.yml} exists at the application level. Deployment is handled by Vercel (Next.js apps), Render (MedusaJS), and EAS (mobile app).

\section{Core Module Implementation}
\subsection{Authentication and Authorization}
The authentication and authorization module is implemented on Supabase Auth, with session validation handled in the tRPC middleware layer of the Next.js API. This module realizes the design documented in Sections 3.4 and 3.5.

\textcolor{red}{\textbf{TODO: Change Inline Ref}} \\

\textbf{Guest (Anonymous) Access.} The guest flow described in Section 3.4.1 is fully implemented. When a user taps "Continue as Guest" on the welcome screen, the application invokes supabase.auth.signInAnonymously() with the Guest role embedded as metadata on the created session. The user is then routed into the main browsing experience as an authenticated anonymous session. Guest users can browse the catalog and add items to the cart but cannot complete bookings or modify profile data, with these restrictions enforced through the role-permissions matrix described below. Figure~\ref{fig:authentication-and-authorization-1} shows the welcome screen and the resulting browsing experience available to guest users.
\thesisfigure{ch4-authentication-and-authorization-1.png}{Guest Access Flow}{Guest 
  Access Flow \\ (a) welcome screen with ``continue as guest'' option, \\ (b) browse tab available to the anonymous session.}{}{authentication-and-authorization-1}

  \textcolor{red}{\textbf{TODO: Change Inline Ref}} \\

\textbf{Essential Registration and Email Verification.} Essential registration follows the flow designed in Section 3.4.2. Email confirmation is hard-enforced at the Supabase configuration level (\texttt{enable\_confirmations = true} in \texttt{supabase/config.toml}), which means no soft-nudge bypass is permitted: a newly registered user is routed to a dedicated verification screen and cannot reach the main applicFigure~\ref{fig:authentication-and-authorization-2}Figure~4.4 shows the sign-up screen, the email verification screen, and the verification email template delivered to the user.
\thesisfigure{ch4-authentication-and-authorization-2.png}{Essential Registration Flow}{Essential Registration Flow \\ (a) sign-up screen, \\ (b) email verification screen, \\ (c) verification email template}{}{authentication-and-authorization-2}

\textbf{Password Reset.} A password reset flow is implemented using Supabase's native password recovery mechanism, with a custom email template rendered from \texttt{supabase/templates/reset\_password.html}. Figure~\ref{fig:authentication-and-authorization-3} shows the forgot-password and reset-password screens as they appear to the user.

\thesisfigure{ch4-authentication-and-authorization-3.png}{Password Reset Flow}{Password Reset Flow (a) forgot-password screen, (b) reset-password screen}{}{authentication-and-authorization-3}

\textbf{JWT Validation in the Next.js API.} Session validation is implemented as a tRPC middleware layer. Each incoming tRPC request carries a bearer token in its authorization header; the \texttt{createTRPCContext} function decodes the token via \texttt{supabase.auth.getUser()} and attaches the resolved user to the request context. The \texttt{protectedProcedure} wrapper then enforces the presence of a valid user on every procedure that requires authentication, rejecting unauthenticated requests before they reach any business logic. This arrangement centralizes the authentication gate in a single layer and eliminates the need for individual procedures to re-validate the session.

\textbf{Role-Based Access Control.} The platform defines four roles through a numeric \texttt{ROLE\_ENUM}: Admin (1), Guest (2), Essential (3), and Elite (4), persisted relationally through a \texttt{roles} table joined to \texttt{users} via a \texttt{user\_roles} association table. Twenty-three discrete permissions are defined in a \texttt{permissions} table and linked to roles through \texttt{role\_permissions}. This relational structure allows permissions to be adjusted without schema changes and supports the tier-specific capabilities documented in Section~3.3.

\textcolor{red}{\textbf{TODO: Change Inline Ref}} \\

\textbf{Row-Level Security.} The RLS design described in Section 3.5.2 is implemented selectively rather than uniformly. RLS policies are applied to four tables where user-scoped data isolation is most critical: \texttt{public\_images}, \texttt{private\_images}, \texttt{user\_roles}, and \texttt{onboarding\_stage}. For remaining tables (including \texttt{profile}, \texttt{roles}, \texttt{permissions}, \texttt{payments}, and itinerary-related tables), authorization is enforced in application code through the \texttt{protectedProcedure} wrapper combined with explicit ownership checks within individual tRPC procedures. This partial application of RLS reflects a pragmatic trade-off: tables where policy logic is straightforward and the security surface is sensitive receive RLS protection, while tables where policy expression would require complex joins or where access patterns are well-contained within application code are authorized at the application layer.

\subsection{User Onboarding and Personalization}
\subsubsection{Tier Routing and Entry Conditions}
The routing logic at the application entry point distinguishes between user states and dispatches users to the appropriate post-authentication surface. Only users in the \texttt{PASSWORD\_SET\_NOT\_ONBOARDED} state reached by Elite users after their invitation has been accepted and their initial payment completed are routed into the onboarding flow. Essential users, already-onboarded Elite users, and Guest sessions are routed directly to the main browsing experience. This implementation confirms the design claim that onboarding is Elite-exclusive, with no Essential-tier onboarding path present in the codebase.

The flow opens with an Elite welcome screen that introduces the purpose of onboarding and offers a "skip for now" affordance. Upon proceeding, the user is presented with the residency question resident or visitor which determines the branch of onboarding that follows. Figure~\ref{fig:tier-routing-and-entry-conditions-1} shows the Elite welcome screen and the residency selection step.
\thesisfigure{ch4-tier-routing-and-entry-conditions-1.png}{Elite Onboarding Entry Point}{Elite Onboarding Entry Point \\ (a) welcome screen with ``start onboarding'' and ``skip for now'' options, (b) residency selection step}{}{tier-routing-and-entry-conditions-1}

\subsubsection{Resident Branch}
Users who select "resident" are presented with a two-step questionnaire focused on establishing their consistent lifestyle patterns within the UAE. Step 2 collects their area of residence, typical weekly routine, lifestyle priorities (e.g., dining, wellness, fitness, private events), types of assistance they seek from the platform, and planning preferences. Step 3 collects dietary needs, preferred communication method, and whether the user wishes to receive curated weekly suggestions. Figure~\ref{fig:resident-branch} shows the resident onboarding screens.

\thesisfigure{ch4-resident-branch.png}{Resident Onboarding Questionnaire}{Resident Onboarding Questionnaire \\ (a) step 2: location, typical week, lifestyle priorities, \\ (b) step 3: dietary, communication, weekly suggestions}{}{resident-branch}

\subsubsection{Visitor Branch}
Users who select "visitor" are presented with a structurally different questionnaire focused on trip logistics rather than routine. Step 2 collects the location and travel dates of their visit, their core interests (dining, wellness, culture, nature, VIP, sports, events, networking, private beaches), their preferred itinerary pace, and any pre-arrival services required (airport transfers, private driver, hotel booking, VIP event tickets, personal assistant). Step 3 collects dietary preferences and any additional requirements.

The visitor branch differs from the resident branch not only in the questions asked but in the underlying data model captured. The two branches write distinct key sets under the \texttt{step\_data} JSONB column, reflecting the different temporal and contextual concerns of each user type. Figure~\ref{fig:visitor-branch} shows the visitor onboarding screens.

\thesisfigure{ch4-visitor-branch.png}{Visitor Onboarding Questionnaire}{Visitor Onboarding Questionnaire \\ (a) step 2: travel dates, interests, pace, services, \\ (b) step 3: dietary preferences and additional requirements}{}{visitor-branch}

\subsubsection{Data Storage and Completion}

Onboarding responses are persisted to a single \texttt{onboarding\_stage} table with the schema (\texttt{user\_id}, \texttt{user\_type}, \texttt{step\_data jsonb}, \texttt{completed\_at}). All preference data — interests, travel dates, dietary restrictions, preferred pace, and so on — is nested under the \texttt{step\_data} field using step-keyed sub-objects (\texttt{step1}, \texttt{step2}, \texttt{step3}). The choice of a single JSONB column rather than a normalized preferences schema reflects the exploratory nature of onboarding data in an early-stage product: the set of questions may evolve as the platform matures, and JSONB storage accommodates schema evolution without migration overhead.

Onboarding completion is tracked through a dual-signal mechanism. When the user submits the final step, the \texttt{completeOnboarding} mutation sets \texttt{onboarding\_completed\_at} on the user's profile record and writes \texttt{completed\_at} on the \texttt{onboarding\_stage} row. The user's resolved state then evaluates to \texttt{ONBOARDED}, which routes subsequent sessions directly to the main application rather than back into the onboarding flow. Every step of the onboarding flow renders a progress bar showing the user's position within the three-step sequence (e.g., ``Step 2 of 3''), providing a clear sense of progression and remaining effort. Figure~\ref{fig:data-storage-and-completion} shows a representative \texttt{onboarding\_stage} row for an onboarded user.
\thesisfigure{ch4-data-storage-and-completion.png}{Stored Onboarding Data in Supabase}{Stored Onboarding Data in Supabase}{}{data-storage-and-completion}
\subsection{Payment System}
\textcolor{red}{\textbf{TODO: Change Inline Ref}} \\
The payment module realizes the architecture documented in Section 3.7, using Stripe as the payment provider and MedusaJS as the commerce backend for order placement. This subsection documents the implemented state of the module across three concerns: one-time experience purchases, Elite subscription billing, and the Elite approval workflow along with an honest inventory of gaps between the original design and the current implementation.

\subsubsection{One-Time Experience Purchases}
One-time purchases support the checkout flow for users booking individual experiences. The implementation spans the mobile client, the tRPC payment router, the Medusa commerce backend, and Stripe's PaymentSheet SDK.

\textcolor{red}{\textbf{TODO: Change Inline Ref ---> 4.10}} \\

The flow begins when a user proceeds from the cart to checkout. The \texttt{useCheckoutFlow} hook on the mobile client splits cart items into internal experiences (charged via Stripe) and external or free items (handled separately), then creates a Medusa payment collection and session representing the transaction. The hook then invokes the mobile Stripe SDK's \texttt{initPaymentSheet} followed by \texttt{presentPaymentSheet}, which renders the native Stripe payment UI for card collection. On successful payment, the client calls a \texttt{completeOrder} endpoint that in turn calls Medusa's \texttt{/store/carts/\{cartId\}/complete} to place the order. Figure 4.10 shows the PaymentSheet as rendered on iOS and Android, and Figure~\ref{fig:one-time-experience-purchases} shows the resulting order confirmation screen.
\thesisfigure{ch4-one-time-experience-purchases.png}{Stripe PaymentSheet on iOS During One-Time Experience Checkout}{Stripe PaymentSheet Rendered on iOS During One-Time Experience Checkout}{}{one-time-experience-purchases}

\subsubsection{Elite Subscription Billing}
The Elite tier requires recurring subscription billing, which is implemented through a Stripe Pricing Table in conjunction with the tRPC payment router. The mobile client renders a plan selection screen that offers both monthly and annual billing intervals, each backed by a distinct Stripe price. The chosen price identifier is passed through to the subscription creation path on the backend.

The payment router implements six procedures to support the subscription surface: \texttt{getUserSubscription} and \texttt{getSubscription} for reading current state, \texttt{createCustomerSession} for initiating the Stripe Pricing Table flow, \texttt{getEliteSubscriptionPrice} for resolving the correct price identifier, \texttt{cancelSubscription} for ending an active subscription, and \texttt{createPaymentIntent} for the one-time flow described above. A subscription record is persisted to the Supabase payments table with \texttt{type = "subscription"}, capturing the Stripe customer identifier, the Stripe subscription identifier, the billing interval, and the mirrored Stripe subscription status.

\section{Product Inventory Implementation}
The product inventory module realizes the design documented in Sections 3.8, 3.9, and the inventory-related parts of Section 3.13. A structural note is important before presenting the implementation detail: the Adnexio platform is delivered as two distinct repositories the consumer-facing monorepo (containing the mobile app, the Next.js API host, and the shared packages), and a separate Medusa backend repository that contains the commerce engine and its custom admin extensions. The authoring tools for internal experiences, the PlatinumList synchronization extension, and the category management UI are implemented within the Medusa backend repository. This subsection therefore documents the inventory module from two complementary perspectives: the authoring surface (evidenced through Medusa admin screenshots) and the consumption surface (evidenced through the mobile client's consumption logic, which resides in the monorepo documented in Chapter 4.1).

\subsection{Internal Experience Management}
Internal experiences are authored through a custom admin extension within the Medusa backend, and are consumed by the mobile client as standard Medusa products carrying typed variants and metadata fields. This subsection documents both sides of that boundary.

\subsubsection{Authoring Surface}
The admin authoring form realizes the metadata schema described in Section 3.9.2. Administrators create internal experiences by filling in a structured form that captures basic information (event title, location, event date, end date), pricing configuration (base price, currency, availability, max capacity), operational constraints (duration, age restriction, weight limit), and policy text (terms and conditions, refund policy). Figure~\ref{fig:authoring-surface-1} and Figure~\ref{fig:authoring-surface-2} show the admin metadata form as it appears to the authoring user.

\thesisfigure{ch4-authoring-surface-1.png}{Internal experience metadata in Medusa admin (1)}{Internal experience metadata in Medusa admin (1)}{}{authoring-surface-1}

\thesisfigure{ch4-authoring-surface-2.png}{Internal experience metadata in Medusa admin (2)}{Internal experience metadata in Medusa admin (2)}{}{authoring-surface-2}

Two distinct pricing models are supported. The \textbf{per-event model} uses a bulk date-range generator that accepts a start date, an end date, a set of default time slots with associated capacities, and a global ticket type catalog (e.g., "Standard Admission", "Ultimate Express"). When the administrator saves, the backend pre-generates a product variant for each combination of (date, time slot, ticket type) a pattern confirmed by the mobile client's variant-grouping logic, which re-groups these pre-generated variants by date and time slot at render time. Figure~\ref{fig:authoring-surface-3} shows the per-event configuration form with the date range generator and ticket type definitions.

\thesisfigure{ch4-authoring-surface-3.png}{Per-event pricing and ticket-type configuration}{Per-event pricing and ticket-type configuration}{}{authoring-surface-3}

The \textbf{rental model} is used for asset-based services such as car rentals. The administrator configures three pricing tiers daily, weekly, and monthly and the backend auto-generates three corresponding seat-category variants upon save. The rental start window is also specified at authoring time to constrain the date range within which users may initiate a rental. Figure~\ref{fig:authoring-surface-4} shows the rental configuration form.

\thesisfigure{ch4-authoring-surface-4.png}{Rental pricing tier configuration}{Rental pricing tier configuration}{}{authoring-surface-4}

\subsubsection{Consumption Surface}
On the mobile side, internal experiences surface through the same product rendering pipeline used for all catalog items. The mobile client reads the following metadata fields from the product record and uses them to shape the user interface:

\begin{itemize}
  \item \texttt{pricing\_model}: distinguishes per-event experiences from rentals, driving which booking configuration is rendered.
  \item \texttt{rental\_pricing}: exposes the three-tier price structure for rental experiences.
  \item \texttt{duration\_hours}, \texttt{venue}, \texttt{max\_capacity}, \texttt{age\_restriction}, \texttt{weight\_limit}, \texttt{additional\_restrictions}: rendered as supporting detail on the experience detail screen.
  \item \texttt{is\_sold\_out}: controls availability on the date picker; when true, the corresponding date slot is rendered as disabled.
  \item \texttt{terms\_text}: conditionally rendered as the mandatory acceptance block on the detail screen.
\end{itemize}

The experience title, base date fields, and starting price are consumed from standard Medusa product and variant fields rather than from metadata.

\textbf{Variant Modeling.} Each product variant is uniquely identified by a (show-time identifier, seat category) combination, with \texttt{show\_time\_start}, \texttt{show\_time\_end}, and \texttt{show\_time\_id} stored in variant metadata. The mobile client's \texttt{groupTimesByDate} logic reads these fields to reconstruct the date-grouped time slot picker at render time, which is how the pre-generated variants produced by the authoring form surface back to the user as a coherent date-and-time selection interface.

\textbf{Rental Duration-to-Tier Mapping.} The mobile booking screen for rental experiences implements the tier-selection rule directly: durations under seven days apply the daily tier, durations between seven and twenty-nine days apply the weekly tier, and durations of thirty days or longer apply the monthly tier. The selected tier is matched to the appropriate product variant by seat category name, and the client updates the displayed price accordingly as the user adjusts the start and end dates.

\textbf{Terms Acceptance Gate.} The terms-acceptance gate described in Section 3.11.1 is fully implemented. When an experience's metadata includes a non-empty \texttt{terms\_text} field, the primary action button on the detail screen is disabled and its label reads ``accept terms to continue'' until the user checks the acceptance box. Once accepted, the button becomes active and its label transitions to the standard booking action.

\subsection{PlatinumList Synchronization}
PlatinumList synchronization comprises two distinct integrations in the platform, which are documented separately to accurately reflect where each integration lives.

\subsubsection{Product Sync (Medusa Backend Extension)}
The core synchronization pipeline described in Section 3.8 fetching event data from the PlatinumList API, transforming it into the platform's product schema, and seeding it into the Medusa catalog is implemented as a custom Medusa admin extension within the separate backend repository. From the perspective of the mobile client and the Next.js API host, PlatinumList-sourced products are indistinguishable from internally authored products except for a \texttt{PL:} prefix on their handle or SKU, which the mobile checkout flow uses to route payment appropriately (discussed below).

The admin extension realizes the manual-sync design choice documented in Section 3.13.4: an administrator initiates synchronization explicitly through the PlatinumList Events page in the Medusa admin, and the extension surfaces a success or failure indicator upon completion. Figure~\ref{fig:product-sync} shows the admin extension interface as rendered in the Medusa admin portal.

\thesisfigure{ch4-product-sync.png}{PlatinumList sync extension in the Medusa admin}{PlatinumList sync extension in the Medusa admin}{}{product-sync}

\subsection{Category and Tag Configuration}
Categories and tags are authored through the Medusa admin and consumed by the mobile client at runtime. This subsection documents both the canonical source of the classification data and how the mobile client renders it.

\subsubsection{Category Authoring and Hierarchy}
The canonical list of categories is maintained within the Medusa backend, with no categories seeded from within this monorepo. Each category record carries a \texttt{metadata.image\_url} field that holds the cover image URL used for visual rendering on the mobile Explore tab, with a fallback image used where no URL is present.

The Medusa query used by the mobile client includes the full descendant tree of each category (\texttt{include\_descendants\_tree: true}), but the mobile client renders only leaf categories, grouped visually under their parent. In other words, although the category hierarchy supports arbitrary depth in theory, the current rendering pattern treats it as a two-level structure for navigation purposes. Figure~\ref{fig:category-authoring-and-hierarchy} shows the Medusa admin categories page on the authoring side and the corresponding mobile Explore tab rendering on the consumption side.

\thesisfigure{ch4-category-authoring-and-hierarchy.png}{Category authoring and consumption}{Category authoring and consumption. \\ (a) Medusa admin categories page showing the configured category list with cover images. \\ (b) Mobile Explore tab rendering the same categories as visual tiles.}{}{category-authoring-and-hierarchy}

\subsubsection{Tag Authoring and Rendering}
Tags are maintained under the Settings > Product Tags section of the Medusa admin. The mobile client fetches the full tag list at runtime with pagination set to fifty tags per page, which is comfortably above the current tag count. Tags are rendered as horizontal filter chips below the search bar on the Explore tab, with the client-injected "all" chip serving as the default selected state. Figure~\ref{fig:tag-authoring-and-rendering} shows the Medusa admin tag list on the authoring side and the rendered filter chip row on the consumption side.
\thesisfigure{ch4-tag-authoring-and-rendering.png}{Tag authoring and consumption}{Tag authoring and consumption. \\ (a) Medusa admin Settings > Product Tags page showing the configured tag list. \\ (b) Mobile Explore tab rendering the same tags as horizontal filter chips below the search bar.}{}{tag-authoring-and-rendering}

\subsubsection{Product Count Rendering}
The mobile Explore tab renders a product count on each category tile to indicate how many experiences the user will find within that category. The count is fetched through the getCategoryProductCounts procedure, which queries the number of products directly assigned to each category identifier. Categories with zero products are hidden from the Explore grid entirely, ensuring users are not led into empty category listings.

A technical note is worth recording: the count reflects direct category assignments only and does not roll up child-category counts into their parent. A parent category whose products are all assigned to its children would therefore show a count of zero even if its subtree contains many experiences.

\section{Booking Flow Implementation}
The booking flow module realizes the five-stage user journey documented in Section 3.12, along with the booking-management surface that follows a completed transaction. The flow is implemented across the mobile client, the tRPC router, and the Medusa commerce backend, and is the primary revenue path of the platform. This subsection documents each stage in the implemented order: detail, configuration, cart, checkout, confirmation, and post-transaction booking management.

\subsection{Experience Detail Screen}
The experience detail screen serves as the first stage of the booking journey and consolidates all information required for the user to make a booking decision into a single consolidated view. Figure~\ref{fig:experience-detail-screen} shows the detail screen as rendered for a representative internal experience.
\thesisfigure{ch4-experience-detail-screen.png}{Experience detail screen}{Experience detail screen}{}{experience-detail-screen}

\noindent\textbf{Content Rendering.} The screen renders the experience title, starting price, location, duration, capacity, age and weight restrictions, transport arrangements, and a rich description block. The image carousel is implemented using a horizontally scrollable list with ten-second auto-play and pagination dots, and tapping an image opens a full-screen image viewer for closer inspection.

\textbf{Terms and Conditions Gate.} As documented in Section 4.3.1.2, the primary action button is disabled and labeled "accept terms to continue" until the user explicitly checks the terms acceptance box, for any experience carrying a non-empty \texttt{terms\_text} metadata field. Only after acceptance does the button become active and transition to the standard booking action label.

\textbf{Contact Lifestyle Assistant.} A secondary action button on the detail screen allows users to contact the lifestyle assistant directly. The behavior is tier-aware: Elite users are routed into WhatsApp through a deep link for direct concierge contact, while non-Elite users are routed to an email-based support channel via a \texttt{mailto:} handler targeting the platform's support address. Toast feedback confirms the action on press.

\subsection{Date, Time, and Ticket Selection}
The configuration stage adapts dynamically to the experience's pricing model, rendering one of the three configurations designed in Section 3.12.1. The variant-grouping logic that underpins this adaptive rendering is documented in Section 4.3.1.2.

\textbf{Dynamic Ticket Categories.} Rather than hard-coding ticket categories, the booking screen extracts the available seat categories directly from the product's variant metadata at render time. The TicketSelector component maps over the filtered set of available categories, rendering one row per category with its own quantity selector and price. This allows the same booking screen to render 

\begin{itemize}
  \item "Adult / Children" for a show-time experience
  \item "General Admission / VIP Express" for a per-event experience
  \item "Daily / Weekly / Monthly" for a rental 
\end{itemize}
This rendering without any configuration-specific conditional logic on the mobile client. Figure~\ref{fig:date-time-and-ticket-selection} shows the three configurations as rendered for representative experiences.
\thesisfigure{ch4-date-time-and-ticket-selection.png}{Date, time, and ticket selection}{Date, time, and ticket selection for (a) open-dates (b) show-time and (c) rental experiences}{}{date-time-and-ticket-selection}

\subsection{Cart}
The cart is a persistent, multi-item structure that retains added experiences across the session and across app restarts. Figure~\ref{fig:cart} shows the cart screen with a representative multi-item booking.

\thesisfigure{ch4-cart.png}{Cart screen showing multi-item booking with reservation timer}{Cart screen showing multi-item booking with reservation timer}{}{cart}

The cart implementation has three key characteristics:
\begin{enumerate}
  \item \textbf{Persistent identity.} The cart identifier is stored in the device's asynchronous storage under a namespaced key. A user who closes and reopens the application returns to the same cart with the same held items, rather than losing state on every app lifecycle event.
  \item \textbf{Multi-item composition.} The cart is structured as a typed array of line items, each carrying its own metadata payload booking date, guest count per category, and special requests. Users may compose a trip of several experiences and check out in a single transaction, as designed in Section 3.12.1.
  \item \textbf{Reservation timer.} A fifteen-minute countdown begins when the first item is added to the cart:
  \begin{enumerate}
    \item The timer ticks once per second and renders prominently at the top of the cart screen.
    \item On expiry, the cart is cleared, a new empty cart is provisioned, and a toast notifies the user that items have been released.
    \item The timer's start timestamp is persisted per cart, so the countdown survives app restarts a user who reopens with three minutes remaining sees three minutes remaining, not a reset clock.
  \end{enumerate}
\end{enumerate}

\subsection{Checkout}
The checkout screen is the intermediate review step between the cart and the Stripe PaymentSheet, implementing the design rationale documented in Section 3.12.1. 
The checkout screen has two responsibilities:
\begin{enumerate}
  \item \textbf{Review surface.} Before committing to payment, the screen presents:
  \begin{itemize}
    \item A consolidated summary of all line items with unit prices, quantities, configured dates, and captured special requests.
    \item A security disclosure confirming that card details are encrypted and not stored on the platform.
    \item A "browse more experiences" link allowing the user to return to the Explore tab.
    \item A Stripe trust indicator at the foot of the summary.
  \end{itemize}
  \item \textbf{Payment initiation.} Tapping the "Pay" button triggers the payment flow documented in Section 4.2.3.1  the cart is split by payment type, a Medusa payment collection is created, and the Stripe PaymentSheet is initialized and presented. Detailed payment architecture is documented there rather than duplicated here.
\end{enumerate}

\subsection{Confirmation}
The confirmation stage communicates the outcome of the transaction and provides navigation actions for continuing on the platform. Figure~\ref{fig:confirmation} shows the confirmation screen as presented after a successful booking.

\thesisfigure{ch4-confirmation.png}{Order confirmation screen}{Order confirmation screen}{}{confirmation}

The confirmation stage is composed of three elements:

\begin{enumerate}
  \item \textbf{Success presentation.} On successful payment, the user is routed to a dedicated confirmation screen containing:
    \begin{itemize}
      \item A success badge and status title (either ``payment successful'' or ``reservation successful'' depending on the order type).
      \item A booking summary enumerating the purchased line items.
      \item A notice confirming that a confirmation email has been dispatched.
      \item Two navigation actions: ``continue browsing'' (returns to Explore) and ``view my bookings'' (routes to My Bookings).
    \end{itemize}

  \item \textbf{Confirmation email dispatch.} Order-placement emails are dispatched through the Medusa backend's notification system (triggered by \texttt{order.placed} events), not through this monorepo's Resend integration. The Resend integration handles a dedicated subset of transactional emails --- subscription cancellation, booking cancellation, and the Elite welcome email. This split reflects the architectural boundary between the commerce engine (order lifecycle) and the application layer (user-account and subscription events).

  \item \textbf{Failure handling.} Payment failures are rendered inline within the Stripe PaymentSheet itself rather than surfaced through a dedicated failure screen, allowing the user to correct their input and reattempt payment without losing context. This is a deliberate design choice documented in Section 3.12.1 and is visible in Figure~\ref{fig:confirmation} from Section 4.2.3.1.
\end{enumerate}

\subsection{My Bookings}
\section{AI Itinerary Builder Implementation}
\subsection{Session Entry and Conversational Flow}
\subsection{Personalization Inputs and Tool-Driven Generation}
\subsection{Generated Itinerary Output and Cart Integration}
\section{Internal Tools Implementation}
\subsection{Customer Management}
\subsection{Order and Refund Management}
\subsection{Sales Dashboard}
\subsection{PlatinumList Extension}
\subsection{Notification System}
\section{AI Itinerary Builder Evaluation}
\subsection{Methodology}
\subsection{Feature Comparison Against AI Travel-Planning Platforms}
